export default function Problem() {
  return (
    <section className="bg-neutral-950 px-6 py-24 text-center text-white">
      <h2 className="mb-6 text-3xl font-semibold">
        Codebases don’t fail because of syntax.
      </h2>

      <p className="mx-auto max-w-2xl text-gray-400">
        They fail because of structure — hidden dependencies, accumulated complexity,
        and silent architectural drift.
      </p>
    </section>
  );
}
