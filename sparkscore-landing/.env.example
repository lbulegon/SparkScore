# URL pública da API Django (obrigatório para proxy de auth e análises).
# Preferir variável SparkScore; o alias SinapLint continua aceite (mesmo backend).
#
# NEXT_PUBLIC_SPARKSCORE_API_URL=https://teu-servico-django.up.railway.app
# NEXT_PUBLIC_SINAPLINT_API_URL=https://legacy-alias.up.railway.app
